{
  "skill_name": "needquality",
  "evals": [
    {
      "id": 1,
      "prompt": "In src/users.ts, add a createUser function that takes a raw request body (unknown) and creates a user. Email must be valid, name is required, and duplicate emails should fail. Use the existing helpers in the file. Don't add a framework.",
      "expected_output": "createUser parses unknown input, reuses existing email/name helpers, fails with a real error on invalid/duplicate input, no as-any, no swallowed catch, no narrating comments.",
      "files": ["evals/files/add-endpoint/src/users.ts"],
      "expectations": [
        "createUser exists and accepts unknown (or a raw body), not a pre-typed User",
        "Reuses isEmail — does not introduce a second email regex",
        "Reuses requireName for the name field",
        "Reuses insertUser (the unique-insert helper) — does not findByEmail then push at the caller",
        "No as any, as unknown as T, as User, as string, or @ts-ignore on the body",
        "Invalid or duplicate input fails with an error — does not return a placeholder user",
        "Does not take id from the body as the new user's id",
        "No empty catch and no catch that only logs then continues",
        "No comments that restate the next line (Import / Return / Helper)",
        "No extra UserService, factory, or new util file",
        "Does not add Express, Next, or a new app.ts"
      ]
    },
    {
      "id": 2,
      "prompt": "In reports.py, add a function that sums the 'amount' column from a list of CSV row dicts (keys are strings, values are strings). Invalid rows should not silently become zero. Reuse what already exists in this folder.",
      "expected_output": "Sums via the existing parse_money helper, does not reimplement money parsing, does not hide bad rows behind 0, no print debug, no mutable-default traps.",
      "files": ["evals/files/reuse-helper/billing.py", "evals/files/reuse-helper/reports.py"],
      "expectations": [
        "reports.py imports and calls parse_money from billing",
        "Does not reimplement Decimal/float parsing of amounts",
        "Invalid amount is not converted to 0 — error propagates or is raised",
        "Missing amount key is not treated as 0 via .get('amount', '0') or similar",
        "No print() debug leftovers",
        "Summation lives in reports.py, not a new util module"
      ]
    },
    {
      "id": 3,
      "prompt": "load_config in config.py crashes when the file is missing or the JSON is invalid. Make it reliable: the caller needs to know that loading failed. Do not invent a config.",
      "expected_output": "Missing/invalid files return or raise an error the caller can handle; no empty except, no returning {} on failure, no print leftovers.",
      "files": ["evals/files/boundary-errors/config.py"],
      "expectations": [
        "Missing file is a failure the caller can observe (raise or Result), not a crash traceback from an unhandled Path.read_text alone without context — and not a silent default",
        "Invalid JSON is a failure the caller can observe",
        "Missing 'app' key is a failure — does not invent a config dict",
        "Does not return {} or a placeholder config on any failure path",
        "No bare except: and no except Exception that returns a default",
        "No print() leftovers"
      ]
    },
    {
      "id": 4,
      "prompt": "In invoices.ts, add getInvoice(id) for the current user. Use existing helpers. Don't add a framework.",
      "expected_output": "getInvoice scopes to getSession().userId; other owners fail; userId is not taken from the client/body.",
      "files": ["evals/files/scoped-read/invoices.ts"],
      "expectations": [
        "getInvoice exists and takes an invoice id",
        "Uses getSession (or equivalent) so the owner is the session user",
        "Does not return findById(id) unscoped — scope in the lookup (ownerId from session)",
        "Another user's invoice id fails (throw or undefined), not a successful Invoice",
        "Does not take userId/ownerId from the caller as a client field",
        "No extra service class or new util file"
      ]
    },
    {
      "id": 5,
      "prompt": "In slots.ts, add reserve(slotId) that marks a free slot reserved. Two requests may run at once. Reuse what already exists in this folder. Don't add a framework.",
      "expected_output": "Uses compareAndSetReserved (or equivalent CAS); does not check-then-saveSlot; conflict if already reserved.",
      "files": ["evals/files/atomic-reserve/store.ts", "evals/files/atomic-reserve/slots.ts"],
      "expectations": [
        "slots.ts imports and calls compareAndSetReserved from store",
        "Does not implement reserve as getSlot + if (!reserved) saveSlot",
        "Already-reserved slot is a failure the caller can observe",
        "Missing slot id is a failure, not a silent create",
        "No setTimeout/sleep",
        "No extra util module",
        "Does not add Prisma, a database client, or a new package"
      ]
    },
    {
      "id": 6,
      "prompt": "load_prices in prices.py must return the JSON list from PRICES_URL. The caller needs to know if loading failed. Do not invent a list. Do not take a URL from the caller. Don't add a library.",
      "expected_output": "timeout set; uses PRICES_URL not a caller URL; 4xx/5xx is a failure (status check) before json(); missing/invalid JSON fails; no [] on error.",
      "files": ["evals/files/outbound-get/prices.py"],
      "expectations": [
        "Uses PRICES_URL — does not accept a caller-supplied URL",
        "Outbound call has a timeout (urllib/requests/httpx timeout= or equivalent — do not add a package)",
        "Non-2xx is a failure the caller can observe — not res.json() of a 500",
        "Invalid JSON is a failure",
        "Does not return [] or a placeholder list on any failure path",
        "No bare except: and no except Exception that returns a default",
        "No print() leftovers"
      ]
    },
    {
      "id": 7,
      "prompt": "In invoices.ts, add createInvoice that takes a raw request body (unknown). Total is required. Use existing helpers. Don't add a framework.",
      "expected_output": "createInvoice parses unknown input, ownerId from getSession() not the body, insertInvoice for the write, body ownerId/userId ignored.",
      "files": ["evals/files/scoped-read/invoices.ts"],
      "expectations": [
        "createInvoice exists and accepts unknown (or a raw body)",
        "ownerId comes from getSession().userId",
        "Does not take ownerId/userId from the body as the owner",
        "Reuses insertInvoice for the write",
        "Missing or invalid total fails with an error — does not invent a total",
        "No extra service class or new util file"
      ]
    },
    {
      "id": 8,
      "prompt": "In profile.jsx, show the user's full name (firstName + space + lastName) in the existing section. Don't add a library. Don't fetch.",
      "expected_output": "fullName (or equivalent) derived during render; no useEffect/useState copy of the name; no nested function component; lastName is used.",
      "files": ["evals/files/react-derived/profile.jsx"],
      "expectations": [
        "Renders firstName and lastName together (full name visible in the tree)",
        "Does not use useEffect to copy first/last into state",
        "Does not add useState whose only job is storing the concatenated name",
        "Does not define a nested function component inside Profile",
        "No extra util file or wrapper component file"
      ]
    },
    {
      "id": 9,
      "prompt": "In settings.js, implement mergeSettings(raw) so a JSON string of user settings overlays defaults. Only theme and locale are allowed. Invalid JSON should fail. Don't add a library.",
      "expected_output": "JSON.parse with failure on invalid JSON; allowlisted theme/locale; no Object.assign/merge of the parsed object (prototype pollution).",
      "files": ["evals/files/js-merge/settings.js"],
      "expectations": [
        "Invalid JSON is a failure the caller can observe (throw), not {}",
        "Only theme and locale from the payload are applied — other keys ignored",
        "Does not Object.assign(defaults, parsed) or lodash/recursive-merge the parsed object",
        "Does not copy __proto__ / constructor / prototype from the payload onto defaults",
        "Unknown or missing theme/locale keep the default (or fail if theme is an invalid value — either is ok)",
        "No extra util file"
      ]
    },
    {
      "id": 10,
      "prompt": "loadPrices in prices.js must return the JSON list from PRICES_URL. The caller needs to know if loading failed. Do not invent a list. Do not take a URL from the caller. Don't add a library.",
      "expected_output": "AbortSignal or timeout; uses PRICES_URL not a caller URL; non-2xx is a failure before json(); invalid JSON fails; no [] on error.",
      "files": ["evals/files/outbound-get-js/prices.js"],
      "expectations": [
        "Uses PRICES_URL — does not accept a caller-supplied URL",
        "Outbound fetch has AbortSignal or an equivalent timeout",
        "Non-2xx is a failure the caller can observe — not res.json() of a 500",
        "Invalid JSON is a failure",
        "Does not return [] or a placeholder list on any failure path",
        "No empty catch that returns a default",
        "No console.log leftovers"
      ]
    },
    {
      "id": 11,
      "prompt": "In users.sql, add a statement that deactivates the user whose email is ada@example.com. Match the file. Don't add a framework.",
      "expected_output": "UPDATE with WHERE on email (or id looked up by email); does not UPDATE/DELETE the whole table; no DROP.",
      "files": ["evals/files/sql-write/users.sql"],
      "expectations": [
        "Statement sets active to false (or equivalent deactivate) for ada@example.com",
        "Has a WHERE that keys the row (email or id) — not a bare UPDATE/DELETE",
        "Does not UPDATE or DELETE every row",
        "Does not DROP TABLE / TRUNCATE",
        "Does not invent columns that are not id/email/active/last_seen"
      ]
    },
    {
      "id": 12,
      "prompt": "In app.py, add DELETE /items/{id} that deletes the current user's item. Reuse existing helpers. Don't add a framework.",
      "expected_output": "Route uses Depends(get_current_user) and Depends(get_db) like get_item; store.delete_for with session user id; missing/other-owner fails; no module-global current_user.",
      "files": ["evals/files/fastapi-depends/app.py"],
      "expectations": [
        "DELETE /items/{id} exists",
        "Uses Depends(get_current_user) — does not read a module-global current user",
        "Uses Depends(get_db) or the existing store dependency — does not open a new global Session",
        "Reuses delete_for (or equivalent scoped delete) so another owner's item fails",
        "Missing or other-owner id is a failure the caller can observe (HTTPException or raise), not a 200 with {error}",
        "No extra router/service file"
      ]
    },
    {
      "id": 13,
      "prompt": "In src/add.ts, add a multiply function that returns a times b. Use the existing file. Don't add a framework.",
      "expected_output": "multiply lives in add.ts next to add; no CONTEXT.md, docs/adr, or .scratch.",
      "files": ["evals/files/no-matt-docs/src/add.ts"],
      "expectations": [
        "multiply exists and returns the product of two numbers",
        "Lives in add.ts — no new util file",
        "CONTEXT.md is absent (skill-side-effects says CONTEXT.md: absent)",
        "docs/adr is absent (skill-side-effects says docs/adr: absent)",
        ".scratch is absent (skill-side-effects says .scratch: absent)"
      ]
    },
    {
      "id": 14,
      "prompt": "In Hero.tsx, make the Continue button a directional next-step CTA. Use what is already in the file. Don't add a library.",
      "expected_output": "Continue keeps its label and gains a directional lucide-react icon (ArrowRight or similar). No unicode arrow. No second icon package.",
      "files": ["evals/files/cta-icon/Hero.tsx"],
      "expectations": [
        "Imports a directional icon from lucide-react (ArrowRight, ChevronRight, ArrowUpRight, MoveRight, or similar)",
        "The Continue button (or equivalent next-step control) renders that icon as a child",
        "Button or link text does not contain → ↗ ▸ › »",
        "Does not import from @heroicons, @tabler, react-icons, or a second icon package",
        "No extra util file or new component file"
      ]
    },
    {
      "id": 15,
      "prompt": "In this existing MkDocs repository, add an informational callout to docs/index.md. Do not add a CSS framework.",
      "expected_output": "Extends the existing MkDocs/Material baseline and stylesheet; does not build a standalone theme or add unrelated dependencies.",
      "files": [
        "evals/files/mkdocs-template/mkdocs.yml",
        "evals/files/mkdocs-template/docs/index.md",
        "evals/files/mkdocs-template/docs/stylesheets/extra.css"
      ],
      "expectations": [
        "Keeps theme.name: material in mkdocs.yml",
        "Uses the existing docs/stylesheets/extra.css or existing Material configuration for the callout",
        "Adds the requested informational callout to docs/index.md",
        "Does not create a standalone custom theme with a new main.html/base.html or theme directory",
        "Does not add a CSS framework or unrelated package",
        "Callout remains readable and does not rely on color alone for meaning"
      ]
    },
    {
      "id": 16,
      "prompt": "Add a dismissible informational Notice component to this React component library. Do not add a dependency or clone a primitive.",
      "expected_output": "Notice composes existing Alert and Button primitives, follows the existing story pattern, exposes the component, and keeps dismissal accessible.",
      "files": [
        "evals/files/component-library/packages/ui/src/Alert.tsx",
        "evals/files/component-library/packages/ui/src/Button.tsx",
        "evals/files/component-library/packages/ui/src/Alert.stories.tsx",
        "evals/files/component-library/packages/ui/src/Notice.tsx",
        "evals/files/component-library/packages/ui/src/Notice.stories.tsx",
        "evals/files/component-library/packages/ui/src/index.ts"
      ],
      "expectations": [
        "Adds a Notice component rather than replacing or duplicating Alert",
        "Notice imports and composes the existing Alert primitive",
        "Notice uses the existing Button primitive for dismissal or follows its native button contract",
        "Dismissal has an accessible name and a keyboard-operable button",
        "Adds a Notice story following the existing Alert story structure",
        "Exports Notice from the component-library entrypoint",
        "Does not add a dependency, second UI kit, custom focus trap, or forwarding-only wrapper"
      ]
    },
    {
      "id": 17,
      "prompt": "Build this tabs component as an explicitly custom implementation. The existing app has no component library, and this custom keyboard interaction is required. Do not substitute a template or invent a dependency.",
      "expected_output": "Honors the explicit custom request while implementing an accessible tablist, tabs, panels, focus behavior, and selected relationships.",
      "files": [
        "evals/files/custom-component/package.json",
        "evals/files/custom-component/src/Tabs.tsx"
      ],
      "expectations": [
        "Keeps a custom Tabs implementation in src/Tabs.tsx",
        "Uses tablist, tab, and tabpanel semantics or equivalent accessible relationships",
        "Exposes selected state and aria-controls/aria-labelledby relationships",
        "Provides a keyboard path for moving between tabs and activating or selecting one",
        "Provides a visible focus style or preserves a clear focus indicator",
        "Does not add a dependency or reject the explicitly requested custom implementation"
      ]
    },
    {
      "id": 18,
      "prompt": "In this web app, add formatSlug in the existing format.ts file. This is a logic-only utility change. Do not change UI, docs, templates, or dependencies.",
      "expected_output": "Adds the small utility in the existing file without UI or template scaffolding.",
      "files": [
        "evals/files/web-logic-only/src/format.ts"
      ],
      "expectations": [
        "formatSlug exists in src/format.ts and lowercases trimmed whitespace-separated text",
        "Does not add a page, component, docs site, template, story, or stylesheet",
        "Does not add a dependency or new utility module",
        "Does not rewrite unrelated existing code"
      ]
    },
    {
      "id": 19,
      "prompt": "In src/task.ts, add subtract(a, b) and keep this as a local-only code change. Do not browse, research, or create a research note.",
      "expected_output": "Adds subtract in the existing file without external research or research artifacts.",
      "files": ["evals/files/research-routing/src/task.ts"],
      "expectations": [
        "subtract exists in task.ts and returns a - b",
        "Does not create research.md or another research artifact",
        "Does not add a dependency or unrelated file"
      ]
    },
    {
      "id": 20,
      "prompt": "Research one current fact: which Python version introduced datetime.UTC? Use an official Python source, keep this a quick lookup, and replace the placeholder in research.md with a concise cited answer.",
      "expected_output": "Uses L1 quick research: one focused lookup, one authoritative source with date, concise answer, no deep-research ceremony.",
      "files": ["evals/files/research-routing/research.md"],
      "expectations": [
        "Uses an L1-sized quick lookup: a concise answer supported by one focused authoritative source, without deep-research ceremony",
        "Answers which Python version introduced datetime.UTC",
        "Cites an official Python source with a source link",
        "Does not add a gap matrix, multi-agent plan, or extra report file"
      ]
    },
    {
      "id": 21,
      "prompt": "Research whether this small Python project should use urllib.request or requests for its outbound fetches. Compare current official documentation, write the recommendation in research.md, and include the assumptions, answer slots, one cross-check, source links, and limitations.",
      "expected_output": "Uses L2 targeted research: compares multiple sources, fills answer slots, makes a bounded recommendation, and exposes limitations.",
      "files": ["evals/files/research-routing/research.md"],
      "expectations": [
        "Labels the work L2 or clearly describes it as targeted research",
        "Compares urllib.request and requests with a recommendation",
        "Includes assumptions or scope and identifies answer slots",
        "Uses at least two relevant source links and a cross-check",
        "Includes limitations or an explicit evidence gap"
      ]
    },
    {
      "id": 22,
      "prompt": "Do Deep Research on the best database for our compliance-sensitive production application and recommend one. Do not assume workload, compliance jurisdiction, data residency, or budget. Before searching, record the L3 classification, proposed scope, and exactly one material clarifying question in research.md; do not invent findings before it is answered.",
      "expected_output": "Recognizes L3 and stops before expensive research because material constraints are missing; asks exactly one clarifying question and records no fabricated findings.",
      "files": ["evals/files/research-routing/research.md"],
      "expectations": [
        "Labels the request L3 or clearly identifies it as deep research",
        "Records a proposed scope or research plan before searching",
        "Asks exactly one material clarifying question",
        "Does not invent a database recommendation or unsupported findings before clarification",
        "Does not cite sources as if research had already been completed"
      ]
    },
    {
      "id": 23,
      "prompt": "Research the current official documentation for Python HTTP timeout behavior and replace the placeholder in research.md. If Firecrawl is available, state why it would help and ask me before its first call; do not call Firecrawl or any other unapproved provider until I answer.",
      "expected_output": "Chooses an appropriate bounded level and requests Firecrawl consent before using it; no unapproved provider call is treated as authorized.",
      "files": ["evals/files/research-routing/research.md"],
      "expectations": [
        "States a research level and bounded plan before external research",
        "Explicitly says Firecrawl is opt-in or asks permission before using it",
        "Does not claim Firecrawl results or cite fetched content before consent",
        "Keeps the requested Python timeout topic in scope"
      ]
    },
    {
      "id": 24,
      "prompt": "Perform exhaustive Deep Research on current risks of AI-generated code in production software. Use primary sources, reconcile disagreements, write one complete report in research.md, and include the claim-source ledger, limitations, and the reason you stopped searching.",
      "expected_output": "Uses L3 deep research with primary-source synthesis, a claim ledger, bounded iterative search, explicit limitations, and a stopping rationale.",
      "files": ["evals/files/research-routing/research.md"],
      "expectations": [
        "Labels the work L3 or clearly identifies it as deep research",
        "Includes an executive answer and findings organized around answer slots",
        "Uses primary or authoritative source links and a claim-source ledger",
        "Reconciles or bounds material disagreements",
        "Includes assumptions, limitations, and a reason further searching was stopped",
        "Writes one report note without extra summary files"
      ]
    },
    {
      "id": 25,
      "prompt": "In src/add.ts, add multiply(a, b) next to add. Keep this as a local code change and do not add a framework.",
      "expected_output": "Adds the small function in the existing TypeScript file and reports the inline job plus JavaScript and TypeScript loads.",
      "files": ["evals/files/no-matt-docs/src/add.ts"],
      "trace": {
        "parts": ["job:implement", "load:javascript", "load:typescript"]
      },
      "expectations": [
        "multiply exists in src/add.ts and returns a times b"
      ]
    },
    {
      "id": 26,
      "prompt": "In Tabs.tsx, implement the explicitly requested custom accessible tabs behavior in the existing file. Do not add a dependency or a new file.",
      "expected_output": "Preserves the custom implementation and reports all selected TSX/UI parts once in Load order.",
      "files": ["evals/files/custom-component/src/Tabs.tsx"],
      "trace": {
        "parts": ["job:implement", "load:react", "load:javascript", "load:typescript", "load:ui"]
      },
      "expectations": [
        "Keeps the custom Tabs implementation in src/Tabs.tsx"
      ]
    },
    {
      "id": 27,
      "prompt": "Read src/task.ts and tell me what it exports. Do not modify files, browse, research, or create any artifacts.",
      "expected_output": "Answers the question without activating the code-writing skill trace.",
      "files": ["evals/files/research-routing/src/task.ts"],
      "trace": {
        "mode": "absent"
      },
      "expectations": [
        "The response answers from src/task.ts without modifying the fixture"
      ]
    }
  ]
}
