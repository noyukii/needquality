export const defaults = {
  theme: "light",
  locale: "en",
}

/** Overlay allowed settings from a JSON string onto defaults. */
export function mergeSettings(raw) {
  throw new Error("not implemented")
}
